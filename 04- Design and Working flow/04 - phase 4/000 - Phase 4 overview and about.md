
```
Phase 4 Stacks
├── cloudwatch.yaml   → Dashboards + Alarms + Log Groups
└── lambda.yaml       → Self-healing Lambda + DR automation
```

## Stack 1 — CloudWatch Dashboards + Alarms

```
payflow-shield/
└── infrastructure/
    └── stacks/
        └── cloudwatch.yaml    ← create this now
```

## Stack 2 — Lambda Self-Healing

```
payflow-shield/
└── infrastructure/
    └── stacks/
        └── lambda.yaml    ← create this now
```

