
```
Phase 5 Stacks
├── github.yaml      → S3 artifact bucket for deployments
└── pipeline.yaml    → CodePipeline + CodeBuild
```


## Stack 1 — S3 Artifact Bucket

```
payflow-shield/
└── infrastructure/
    └── stacks/
        └── pipeline-bucket.yaml    ← create this now
```

## Stack 2 — CodePipeline + CodeBuild

```
payflow-shield/
└── infrastructure/
    └── stacks/
        └── pipeline.yaml    ← create this now
```

